<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

  <!-- "Delete Note", it should appear on Edit Note screen -->
  <item
      android:id="@+id/action_delete_note"
      android:icon="@drawable/ic_delete_white_24dp"
      android:title="Delete Note"
      app:showAsAction="always"/>

  <!-- "Save changes", it should appear on Add Note screen -->
  <item
      android:id="@+id/action_save_changes"
      android:icon="@drawable/ic_check_white_24dp"
      android:title="Save Changes"
      app:showAsAction="always"/>

</menu>